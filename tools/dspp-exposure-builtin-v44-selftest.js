// Fix 3.3.10.44 -- built-in local tools no longer count toward the MCP auto-degrade trigger;
// DeepSeek finish_reason=rate_limit_reached is surfaced and stops the run explicitly.
// Usage: node dspp-exposure-builtin-v44-selftest.js [ROOT]   (defaults to the directory of this file)
const fs = require('fs'), path = require('path'), vm = require('vm');
const root = process.argv[2] || __dirname;
const bg = fs.readFileSync(path.join(root, 'background.js'), 'utf8');
const cjs = fs.readFileSync(path.join(root, 'content-scripts', 'content.js'), 'utf8');
const polSrc = fs.readFileSync(path.join(root, 'fix3-policy.js'), 'utf8');
let pass = 0, fail = 0;
function ok(name, cond, detail) { console.log((cond ? 'PASS ' : 'FAIL ') + name + (detail ? ' ' + detail : '')); cond ? pass++ : fail++; }
function grabFn(src, name) {
  const i = src.indexOf('function ' + name + '(');
  if (i < 0) throw new Error('missing ' + name);
  let depth = 0, j = src.indexOf('{', i);
  for (; j < src.length; j++) { if (src[j] === '{') depth++; else if (src[j] === '}' && --depth === 0) break; }
  return src.slice(i, j + 1) + '\n';
}
// The real ShunCode 15-tool schema (tools/list of 2026-09-18), frozen so the cost stays 49,240 B.
const SHUNCODE = JSON.parse(`[{"name":"apply_patch","title":"Apply Patch","description":"Apply a Codex-style multi-file patch directly inside the current workspace. Use this as the primary edit tool for GPT-family models. Prefer one apply_patch call containing all related file edits instead of many small write calls. Keep changes modular. If new behavior can live in a nearby helper, utility, component, or service, prefer that over lengthening an already multi-purpose block. Patch syntax starts with '*** Begin Patch' and ends with '*** End Patch'. Supported directives are '*** Update File:', optional immediate '*** Move to:', '*** Add File:', and '*** Delete File:'. Update hunks start with '@@' and use unchanged lines prefixed by one space, removed lines prefixed by '-', and added lines prefixed by '+'. Update hunk old/context lines must match exactly and uniquely. If context is stale or ambiguous the patch fails without partial application; re-read the file and regenerate the patch. When read_files has returned version hashes for files you are modifying, pass them in expected_versions keyed by file path. A mismatch returns STALE_FILE instead of editing a file that changed after it was read. Do not avoid editing a large file when the requested change belongs there; instead, keep reads and hunks tightly scoped to the owning section and extract adjacent helpers only when they genuinely improve structure. All supported patch operations, including add, update, delete, move, and whole-file replacement, run after the same workspace-boundary, version, lock, staging, and rollback validation; no additional interactive approval is required. The runtime preflights the entire patch before mutation, serializes concurrent writes per affected file, preserves UTF-8 BOM and existing CRLF/LF style for updated files, stages all new file images before commit, and installs each written file atomically when the filesystem supports same-directory rename/link semantics. The returned diff is a canonical unified diff generated by the runtime from the validated old/new file contents, not an echo of the model-supplied patch. Multi-file commits are staged and rollback-capable but are not claimed to be one filesystem-wide atomic transaction.","inputSchema":{"type":"object","properties":{"patch":{"type":"string","minLength":1,"description":"Codex-style patch text using *** Begin Patch / *** End Patch and Add/Update/Delete/Move directives."},"expected_versions":{"type":"object","additionalProperties":{"type":"string","pattern":"^sha256:"},"description":"Optional map from existing file paths to sha256:... versions previously returned by read_files. Strongly recommended whenever those versions are available."}},"required":["patch"],"additionalProperties":false}},{"name":"find_files","title":"Find Files","description":"Find files by path/name glob patterns inside the current workspace; this does not search file contents. Use find_files when you know a filename, extension, or path shape but not the exact path. Use search_files when you need to search file contents. Use it for fuzzy filename/path discovery inside the workspace. Batch independent file patterns together in the patterns array instead of making separate calls. Patterns are evaluated relative to path, which defaults to the current local workspace root opened in ShunCode. Basename-only patterns such as '*.ts' match recursively within that scope. Results are files only, never directories. Returned file paths and the reported scope are normalized to workspace-relative paths even when path was supplied as an absolute in-workspace directory. By default matching is case-insensitive, ignored/common generated directories and hidden paths are skipped, and results are sorted by modification time newest first. Use exclude for additional path globs, include_hidden/no_ignore only when those files are intentionally needed, and sort='path_asc' when deterministic path order matters. If the conversation points to a sibling checkout, external-disk folder, or approximate local path outside the workspace, use run_command with ls/find/grep there instead of assuming find_files can see it. Results are hard-bounded: at most 5000 candidate paths are collected internally before sorting, and by default only 100 paths are returned (hard maximum 500). If truncated=true, narrow path/patterns before increasing max_results.","inputSchema":{"type":"object","properties":{"patterns":{"type":"array","minItems":1,"maxItems":20,"items":{"type":"string","minLength":1},"description":"One or more glob patterns to find in a single call, e.g. ['**/*-files.ts', '**/mcp-server.ts']."},"path":{"type":"string","description":"Optional workspace directory scope inside the currently open local ShunCode folder(s). Workspace-relative is preferred; an absolute path is accepted only when it remains inside an allowed workspace root for this session. Defaults to '.'."},"exclude":{"type":"array","maxItems":50,"items":{"type":"string","minLength":1},"description":"Optional glob patterns to exclude, evaluated relative to the requested path scope."},"case_sensitive":{"type":"boolean","description":"Whether glob matching is case-sensitive. Defaults to false."},"no_ignore":{"type":"boolean","description":"Set true to bypass ignore files/common generated-directory excludes. Defaults to false."},"include_hidden":{"type":"boolean","description":"Set true to include hidden files/directories. Defaults to false."},"max_results":{"type":"integer","minimum":1,"maximum":500,"description":"Maximum file paths returned. Defaults to 100; hard maximum 500."},"sort":{"type":"string","enum":["modified_desc","path_asc"],"description":"Result order. Defaults to modified_desc (newest first); path_asc gives deterministic lexical order."}},"required":["patterns"],"additionalProperties":false}},{"name":"read_files","title":"Read Files","description":"Read one or more UTF-8 text files from the current local workspace opened in ShunCode. These file tools are workspace-scoped. If the user clearly points to another local path on this same Windows host outside the open workspace, inspect that path with run_command first instead of treating workspace-only results as proof of absence. Batch independent files together in one call. For small files, omit start_line/end_line to read the complete file. For large files, results may be truncated and include next_start_line. For large-file work, prefer search_files and explicit line ranges so you read only the sections that matter before expanding scope. A satisfied explicit range can still report has_more=true when the file continues afterward. Use 1-based inclusive start_line/end_line for targeted reads. Successful result paths are normalized to workspace-relative paths even when the request used an absolute in-workspace path. Files at or above the very-large-file threshold require an explicit range. Smaller files may still be automatically truncated by per-file line/byte/token budgets; prefer search_files before targeted reads when location is unknown.","inputSchema":{"type":"object","properties":{"files":{"type":"array","minItems":1,"maxItems":20,"description":"Files to read. Independent files should be requested together.","items":{"type":"object","properties":{"path":{"type":"string","description":"Workspace file path inside the currently open local ShunCode folder(s). Workspace-relative is preferred; an absolute path is accepted only when it remains inside an allowed workspace root for this session."},"start_line":{"type":"integer","minimum":1,"description":"Optional 1-based inclusive first line."},"end_line":{"type":"integer","minimum":1,"description":"Optional 1-based inclusive last line."}},"required":["path"],"additionalProperties":false}}},"required":["files"],"additionalProperties":false}},{"name":"read_image","title":"Read Image","description":"Read an image file from the current local workspace opened in ShunCode and inspect its visual metadata and content. Supported image formats include PNG, JPEG, GIF, WebP, BMP, SVG, and ICO. Returns image dimensions (width, height), aspect ratio, format, MIME type, file size, and base64 data URI. Use this to inspect UI mockups, icons, screenshots, diagrams, and visual assets before implementing or verifying designs.","inputSchema":{"type":"object","properties":{"path":{"type":"string","minLength":1,"description":"Workspace image path inside the currently open local ShunCode folder(s). Workspace-relative is preferred."},"include_data_uri":{"type":"boolean","description":"Whether to include the base64 data URI in the response. Defaults to true."}},"required":["path"],"additionalProperties":false}},{"name":"search_files","title":"Search Files","description":"Search UTF-8 text file contents inside the current workspace and return bounded path/line/snippet matches. Use this to locate relevant code before calling read_files. Use this to narrow large files to the relevant regions before broader reads or edits. This search is workspace-scoped; if the user points to another local path on the same Windows host outside the open workspace, inspect that path with run_command first. Literal search is the default; set is_regex=true only when regular-expression semantics are required. Omit case_sensitive for smart-case (lowercase patterns are case-insensitive; uppercase makes the search case-sensitive). Use path to narrow the directory/file scope and include/exclude glob arrays to filter files. Returned match paths and the reported scope are normalized to workspace-relative paths even when path was supplied as an absolute in-workspace path. context_lines returns nearby lines for disambiguation; keep it small because search is for locating code, not reading whole files. Results are hard-bounded by per-file/global/output budgets. If truncated=true, narrow the query and search again. By default ignored/common generated directories and hidden paths are skipped.","inputSchema":{"type":"object","properties":{"pattern":{"type":"string","minLength":1,"description":"Text or regex pattern to search for. Literal text by default."},"path":{"type":"string","description":"Optional workspace file or directory scope inside the currently open local ShunCode folder(s). Workspace-relative is preferred; an absolute path is accepted only when it remains inside an allowed workspace root for this session. Defaults to '.'."},"is_regex":{"type":"boolean","description":"Set true to interpret pattern as a regular expression. Defaults to false (literal search)."},"case_sensitive":{"type":"boolean","description":"Optional case mode. true=sensitive, false=insensitive, omitted=smart-case."},"include":{"type":"array","items":{"type":"string","minLength":1},"description":"Optional glob filters evaluated relative to the requested path scope, e.g. ['**/*.ts', '**/*.tsx']."},"exclude":{"type":"array","items":{"type":"string","minLength":1},"description":"Optional glob filters evaluated relative to the requested path scope, e.g. ['**/*.test.ts']."},"context_lines":{"type":"integer","minimum":0,"maximum":5,"description":"Surrounding lines on each side of each match. Defaults to 1, maximum 5."},"max_results":{"type":"integer","minimum":1,"maximum":500,"description":"Maximum matches returned across the call. Defaults to 100; hard maximum 500."},"max_matches_per_file":{"type":"integer","minimum":1,"maximum":100,"description":"Maximum matches returned from one file. Defaults to 20; hard maximum 100."},"no_ignore":{"type":"boolean","description":"Set true to ignore .gitignore/common excludes. Defaults to false."},"include_hidden":{"type":"boolean","description":"Set true to include hidden files/directories. Defaults to false."}},"required":["pattern"],"additionalProperties":false}},{"name":"list_directory","title":"list_directory","description":"List the immediate contents of a workspace directory. Use this to understand what is in a known directory; use find_files when searching by filename/path pattern. Depth is intentionally limited to 1 or 2.","inputSchema":{"type":"object","properties":{"path":{"type":"string","description":"Workspace-relative directory path. Defaults to the workspace root."},"depth":{"type":"integer","enum":[1,2],"default":1,"description":"Directory depth to list. Keep this small; use find_files for recursive discovery."},"include_hidden":{"type":"boolean","default":false,"description":"Include dot-prefixed entries."},"no_ignore":{"type":"boolean","default":false,"description":"Include common generated/ignored directories such as node_modules, dist and .git."},"max_entries":{"type":"integer","minimum":1,"maximum":500,"default":200,"description":"Maximum returned entries."}},"additionalProperties":false}},{"name":"run_command","title":"Run Command","description":"Run a shell command, including a multiline script, on the same Windows host that runs the current ShunCode workspace in a ShunCode-managed persistent real PTY backed by node-pty/ConPTY. Windows PTY spawns the product-bundled PortableGit bash.exe with --noprofile --norc. There is no PowerShell fallback: a missing bundled Bash is a packaging/install error. Pass native Bash source directly and use Bash quoting, pipelines and here-docs. The bundled Unix toolchain includes git, grep, sed, awk, find and curl. Inside Bash prefer /c/... paths for Windows drives; call powershell.exe explicitly only for Windows-only facilities such as registry, services, event log or .ps1 scripts. Idle terminal closes after 2h; a fresh shell loses prior variables and cwd. Shell state persists while the same owner-scoped terminal remains available. Omit cwd to continue from the most recently used idle terminal; a fresh terminal starts at the workspace root. Interactive input and background processes are supported. background defaults to false. Returns a cryptographically random owner-scoped command_id capability for later output, input, or cancellation. When shuncode.terminal.pipelineExitCodes is enabled (default), Bash pipelines additionally report pipeline_exit_codes.","inputSchema":{"type":"object","required":["command"],"properties":{"command":{"type":"string","minLength":1,"description":"Native Bash command or multiline Bash script."},"cwd":{"type":"string","description":"Optional workspace-relative working directory. When omitted, reuse the most recently used idle ShunCode terminal and continue from its current directory; a new terminal starts at the workspace root."},"background":{"type":"boolean","default":false,"description":"Whether this is expected to keep running. Defaults to false."},"timeout_ms":{"type":"integer","minimum":1000,"maximum":120000,"default":120000,"description":"For foreground commands, maximum time to wait before returning status=running. The command is not killed on timeout."}},"additionalProperties":false}},{"name":"get_command_output","title":"Get Command Output","description":"Read new output, status and exit_code from a previously started run_command using its exact opaque command_id in the same native Chat or Bridge MCP session scope. Use next_offset on subsequent reads to avoid repeating old output.","inputSchema":{"type":"object","required":["command_id"],"properties":{"command_id":{"type":"string","minLength":1},"offset":{"type":"integer","minimum":0,"default":0,"description":"Absolute UTF-8 byte offset into captured output."},"max_bytes":{"type":"integer","minimum":1,"maximum":131072,"default":32768}},"additionalProperties":false}},{"name":"cancel_command","title":"Cancel Command","description":"Cancel a managed command using its exact cryptographically random command_id in the same native Chat or Bridge MCP session scope. The first request sends a soft foreground interrupt and waits for grace_ms. If force_required remains true, force=true may close only that managed terminal. High-risk packaging, signing, installation, migration, archive mutation/restore and bulk-move operations require a host-owned local confirmation; the AI cannot approve it.","inputSchema":{"type":"object","required":["command_id"],"properties":{"command_id":{"type":"string","minLength":1},"grace_ms":{"type":"integer","minimum":100,"maximum":10000,"default":1500},"force":{"type":"boolean","default":false}},"additionalProperties":false}},{"name":"send_command_input","title":"Send Command Input","description":"Send text to the terminal of a running command. Use for interactive prompts or REPL input. A newline is appended by default.","inputSchema":{"type":"object","required":["command_id","input"],"properties":{"command_id":{"type":"string","minLength":1},"input":{"type":"string"},"append_newline":{"type":"boolean","default":true}},"additionalProperties":false}},{"name":"get_diagnostics","title":"get_diagnostics","description":"Read current diagnostics from VS Code and active language services, including unsaved editor state when providers report it. Use after edits/builds to inspect errors and warnings structurally instead of parsing compiler output when diagnostics are available.","inputSchema":{"type":"object","properties":{"path":{"type":"string","description":"Optional workspace-relative file or directory scope."},"severity":{"type":"array","items":{"type":"string","enum":["error","warning","information","hint"]},"uniqueItems":true,"description":"Optional severity filter. Defaults to all severities."},"max_results":{"type":"integer","minimum":1,"maximum":500,"default":100}},"additionalProperties":false}},{"name":"lsp","title":"lsp","description":"Navigate code semantically through the language services already active in VS Code. Use this for code symbols rather than text search: workspace/document symbols, go-to-definition, references, implementations, and hover/type information. Results include provider_state, project_anchor, project_anchor_source, warmup_performed, and semantic_result_inconclusive metadata so empty semantic results and heuristic warm-up anchors are not over-interpreted. Use search_files for raw text and read_files after lsp locates the relevant implementation.","inputSchema":{"type":"object","required":["operation"],"properties":{"operation":{"type":"string","enum":["workspace_symbols","document_symbols","definition","references","implementation","hover"],"description":"Semantic operation to execute through VS Code language feature providers."},"path":{"type":"string","description":"Workspace source path. Workspace-relative is preferred; absolute paths are accepted only when they remain inside the workspace. Required for document_symbols/definition/references/implementation/hover. Optional for workspace_symbols as a project/file/directory anchor to activate the relevant language project before semantic search."},"line":{"type":"integer","minimum":1,"description":"1-based source line. Required for definition/references/implementation/hover."},"column":{"type":"integer","minimum":1,"description":"1-based UTF-16 source column. Required for definition/references/implementation/hover."},"query":{"type":"string","description":"Symbol query. Required for workspace_symbols."},"include_declaration":{"type":"boolean","default":true,"description":"For references, include the symbol declaration/definition when present."},"max_results":{"type":"integer","minimum":1,"maximum":500,"description":"Maximum returned semantic results. Operation-specific defaults are used when omitted."}},"additionalProperties":false}},{"name":"set_todos","title":"Set Todos","description":"Set the complete durable task list for the current remote-agent job in ShunCode. Use this for multi-step work so the local user can see what is done, in progress, and still pending. Send the full list whenever the plan changes; keep at most one item in_progress. Immediately before a final answer, send the terminal snapshot and wait for acknowledgement; if the work is complete every item must be completed. Use report_progress for transient details instead of creating tool-call-sized todos. Send an empty list to clear task state.","inputSchema":{"type":"object","required":["todos"],"properties":{"todos":{"type":"array","maxItems":24,"description":"Complete ordered todo snapshot for the current job.","items":{"type":"object","required":["id","title","status"],"properties":{"id":{"type":"string","minLength":1,"maxLength":80,"description":"Stable id reused across later set_todos updates."},"title":{"type":"string","minLength":1,"maxLength":400,"description":"Goal-level task title, not an individual tool call."},"status":{"type":"string","enum":["pending","in_progress","completed"],"description":"Current todo state. Keep at most one todo in_progress."}},"additionalProperties":false}}},"additionalProperties":false}},{"name":"update_plan","title":"Update Plan","description":"Compatibility plan updater for the current remote-agent job in ShunCode. It accepts the Codex-style plan array and publishes it to the same durable task card as set_todos. This must be the Bridge MCP tool call: updating only a client-local plan does not update ShunCode. Send the full plan whenever state changes, and immediately before a final answer send the terminal snapshot and wait for acknowledgement. When work is complete every item must be completed; when blocked or incomplete keep truthful non-terminal statuses.","inputSchema":{"type":"object","required":["plan"],"properties":{"explanation":{"type":"string","maxLength":2000,"description":"Optional concise reason for the plan update; task-card state is derived from plan."},"plan":{"type":"array","maxItems":24,"description":"Complete ordered plan snapshot for the current job.","items":{"type":"object","required":["step","status"],"properties":{"step":{"type":"string","minLength":1,"maxLength":400,"description":"Goal-level step title displayed in the ShunCode task card."},"status":{"type":"string","enum":["pending","in_progress","completed"],"description":"Current step status. Keep at most one item in_progress."}},"additionalProperties":false}}},"additionalProperties":false}},{"name":"report_progress","title":"Report Progress","description":"Report concise transient progress from the remote MCP agent to the ShunCode Bridge UI. For multi-step work, maintain durable task state with set_todos and use report_progress for what you are doing right now. todo_id is optional: when omitted, ShunCode automatically associates progress with the sole in_progress todo. This tool does not modify workspace files.","inputSchema":{"type":"object","required":["message"],"properties":{"message":{"type":"string","minLength":1,"maxLength":2000,"description":"Human-readable progress update."},"phase":{"type":"string","maxLength":160,"description":"Optional short phase label, such as Reading, Editing, Testing, or Done."},"percent":{"type":"integer","minimum":0,"maximum":100,"description":"Optional completion estimate from 0 to 100 for the current activity/todo."},"todo_id":{"type":"string","minLength":1,"maxLength":80,"description":"Optional todo id from set_todos. Omit when there is exactly one in_progress todo; ShunCode will link it automatically."}},"additionalProperties":false}}]`);
const SERVER = '9a351af5-0998-48c5-b6be-7f0d1ee53257';
function scDesc(t, serverId = SERVER, enabled = true) {
  return { id: 'mcp:' + serverId + ':' + t.name, name: t.name, invocationName: 'mcp_t_' + serverId.replace(/-/g, '_') + '_' + t.name, title: t.title, description: t.description, inputSchema: t.inputSchema,
    provider: { kind: 'mcp', id: serverId, displayName: 'shuncode', transport: 'streamable_http' }, execution: { mode: 'auto', enabled, risk: 'medium', timeoutMs: 120000, maxResultBytes: 128000 },
    annotations: { mcpServerId: serverId, mcpToolName: t.name } };
}
function catalogDesc(op) { return { id: 'local:mcp_capability:mcp_' + op, provider: { kind: 'local', id: 'mcp_capability', displayName: 'MCP Capability', transport: 'in_process' }, name: 'mcp_' + op, invocationName: 'mcp_' + op, title: op, description: 'catalog ' + op, inputSchema: { type: 'object', properties: {} }, execution: { mode: 'auto', enabled: true, risk: 'low' }, annotations: { 'dpp.mcpCapabilityOperation': op } }; }

// ---- background.js extraction: minified definitions pulled on demand into one vm context ----
function balanced(src, i, open, close) { let d = 0; for (let j = i; j < src.length; j++) { const ch = src[j]; if (ch === '`') { j++; while (j < src.length && src[j] !== '`') { if (src[j] === '\\') j++; j++; } continue; } if (ch === open) d++; else if (ch === close && --d === 0) return j; } return -1; }
function bgFn(name) { const re = new RegExp('(?:^|[;{}\\s,)])((?:async )?function ' + name.replace(/\$/g, '\\$') + '\\()'); const m = re.exec(bg); if (!m) return null; const i = m.index + m[0].length - m[1].length; const b = bg.indexOf('{', bg.indexOf(')', i)); return bg.slice(i, balanced(bg, b, '{', '}') + 1); }
function bgVar(name) { const re = new RegExp('(?:var |,|;)' + name.replace(/\$/g, '\\$') + '=(?!=)', 'g'); let m; while ((m = re.exec(bg))) { const i = m.index + m[0].length, ch = bg[i]; let e; if (ch === '{') e = balanced(bg, i, '{', '}'); else if (ch === '[') e = balanced(bg, i, '[', ']'); else if (bg.startsWith('new Set(', i) || bg.startsWith('Object.freeze(', i)) e = balanced(bg, bg.indexOf('(', i), '(', ')'); else if (ch === '`') { e = i + 1; while (bg[e] !== '`') { if (bg[e] === '\\') e++; e++; } } else { let d = 0; for (e = i; e < bg.length; e++) { const c = bg[e]; if (c === '(' || c === '[' || c === '{') d++; else if (c === ')' || c === ']' || c === '}') { if (d === 0) break; d--; } else if ((c === ',' || c === ';') && d === 0) break; } e--; } return 'var ' + name + '=' + bg.slice(i, e + 1) + ';'; } return null; }
const bgCtx = { console, TextEncoder, TextDecoder, JSON, Math, Number, String, Array, Object, Date, Map, Set, RegExp, Error, chrome: { runtime: { getManifest: () => ({ version: 'x' }) } } };
bgCtx.globalThis = bgCtx; vm.createContext(bgCtx); vm.runInContext(polSrc, bgCtx);
const defined = new Set();
function need(name) { if (defined.has(name)) return true; const code = bgFn(name) || bgVar(name); if (!code) return false; defined.add(name); for (let k = 0; k < 80; k++) { try { vm.runInContext(code, bgCtx); return true; } catch (e) { const mm = /(\w+|\$\w*) is not defined/.exec(e.message); if (!mm || !need(mm[1])) throw e; } } return false; }
function evalBg(expr) { for (let k = 0; k < 120; k++) { try { return vm.runInContext(expr, bgCtx); } catch (e) { const mm = /(\w+|\$\w*) is not defined/.exec(e.message); if (!mm || !need(mm[1])) throw e; } } throw new Error('unresolved: ' + expr); }
function callBg(name, arg) { bgCtx.__dppArg = arg; return evalBg(name + '(__dppArg)'); }
const localZh = ['Lt', 'dx', 'oy', 'Ty', 'qy'].flatMap(fn => evalBg(fn + '("zh-CN")'));
const localEn = ['Lt', 'dx', 'oy', 'Ty', 'qy'].flatMap(fn => evalBg(fn + '("en")'));
const p = bgCtx.DPP_FIX3;
const cost = xs => xs.reduce((a, x) => a + p.promptDescriptorCost(x), 0);
const shuncode = SHUNCODE.map(t => scDesc(t));
const scCost = cost(shuncode), zhCost = cost(localZh), enCost = cost(localEn);

// [1] the field numbers
ok('built-in local descriptors extracted from background.js (9 zh-CN / 9 en, all provider.kind=local)', localZh.length === 9 && localEn.length === 9 && [...localZh, ...localEn].every(d => d.provider.kind === 'local' && d.execution.enabled === true), localZh.map(d => d.name).join(','));
ok('ShunCode 15-tool prompt cost is the real 49,240 B', scCost === 49240, String(scCost));
ok('built-in tools cost 15-20 KB (zh-CN ' + zhCost + ' / en ' + enCost + ')', zhCost > 15000 && zhCost < 20000 && enCost > 15000 && enCost < 20000);
ok('.43 accounting (built-ins counted) crossed the 64,000 trigger', scCost + zhCost > 64000 && scCost + enCost > 64000, String(scCost + zhCost));
ok('.44 accounting (MCP only) stays under the trigger', scCost <= 64000);

// [2] policy
const unset = { version: 1, adaptiveMaxDirectTools: 8, adaptiveMaxPromptBytes: 24000, servers: {} };
ok('policy literal counts MCP descriptors only', polSrc.includes('let enabled=descriptors.filter(d=>d?.execution?.enabled!==false&&d?.provider?.kind===`mcp`&&!!d.provider.id),total=enabled.reduce((sum,d)=>sum+promptDescriptorCost(d),0),'));
ok('old .43 total literal gone', !polSrc.includes('let enabled=descriptors.filter(d=>d?.execution?.enabled!==false),total='));
ok('trigger literal still 64000', polSrc.includes('triggerBudget=Math.max(64000,adaptiveBudget*2)'));
ok('explicit-mode guard (.42) still present', polSrc.includes('(current&&typeof current.mode===`string`))continue;'));
for (const [label, locals] of [['zh-CN', localZh], ['en', localEn]]) {
  const r = p.promptExposureSettings([...locals, ...shuncode, ...['discover', 'describe', 'invoke'].map(catalogDesc)], unset, '继续');
  ok('single ShunCode server + built-ins (' + label + ') with UNSET mode stays direct (identity settings)', r === unset);
}
const explicitDirect = { ...unset, servers: { [SERVER]: { mode: 'direct', pinnedDescriptorIds: [] } } };
ok('explicit direct still respected', p.promptExposureSettings([...localZh, ...shuncode], explicitDirect, '继续') === explicitDirect);
const two = [...shuncode, ...SHUNCODE.map(t => scDesc(t, 'second-server'))];
const r2 = p.promptExposureSettings([...localZh, ...two], unset, '继续');
ok('two ShunCode-sized servers (98,480 B) with unset mode are still auto-degraded', r2 !== unset && r2.servers[SERVER].mode === 'adaptive' && r2.servers['second-server'].mode === 'adaptive');
ok('unset settings object not mutated', unset.servers[SERVER] === undefined);
const disabledTwo = [...shuncode, ...SHUNCODE.map(t => scDesc(t, 'second-server', false))];
ok('disabled MCP descriptors are not counted', p.promptExposureSettings(disabledTwo, unset, '继续') === unset);
const noId = [...shuncode, ...SHUNCODE.map(t => { const d = scDesc(t, 'x'); d.provider = { kind: 'mcp', id: '' }; return d; })];
ok('MCP descriptors without provider.id are not counted', p.promptExposureSettings(noId, unset, '继续') === unset);
const bigLocal = Array.from({ length: 40 }, (_, i) => ({ ...localZh[0], id: 'local:x:' + i, name: 'x' + i, invocationName: 'x' + i, description: 'y'.repeat(3000) }));
ok('local-only descriptors never trigger a rewrite', p.promptExposureSettings(bigLocal, unset, '继续') === unset);
ok('non-object / missing settings pass through', p.promptExposureSettings([...localZh, ...shuncode], null, '继续') === null);

// [3] background _d() pipeline: direct keeps 15 ShunCode tools; the adaptive rewrite reproduces the .43 field count 20
{
  const descriptors = [...localZh, ...shuncode, ...['discover', 'describe', 'invoke'].map(catalogDesc)];
  const direct = callBg('_d', { descriptors, settings: p.promptExposureSettings(descriptors, unset, '继续'), intent: '继续' });
  ok('_d() direct: 24 prompt descriptors (9 local + 15 ShunCode), no catalog', direct.descriptors.length === 24 && direct.directDescriptorIds.length === 15 && direct.usesCatalog === false, String(direct.descriptors.length));
  ok('_d() direct keeps find_files in server order', direct.descriptors.slice(9, 12).map(d => d.name).join(',') === 'apply_patch,find_files,read_files');
  const degraded = { ...unset, servers: { [SERVER]: { mode: 'adaptive', pinnedDescriptorIds: [] } } };
  const adaptive = callBg('_d', { descriptors, settings: degraded, intent: '继续' });
  ok('_d() adaptive reproduces the .43 field shape: 20 = 9 local + 8 slots + 3 catalog', adaptive.descriptors.length === 20 && adaptive.directDescriptorIds.length === 8 && adaptive.usesCatalog === true, String(adaptive.descriptors.length));
  ok('_d() adaptive drops find_files (the drift the user saw)', adaptive.descriptors.slice(9, 12).map(d => d.name).join(',') === 'apply_patch,read_files,read_image');
}

// [4] content.js: finish_reason visibility + explicit rate-limit stop
{
  const src = grabFn(cjs, 'DPP_STREAM_DIAG_FRAME_33') + grabFn(cjs, 'DPP_FINISH_REASON_331044') + grabFn(cjs, 'DPP_LAST_FINISH_REASON_331044') + grabFn(cjs, 'DPP_RATE_LIMIT_STOP_331044') + grabFn(cjs, 'DPP_GENERIC_NUDGE_LIMIT_331025');
  const c = { console, JSON, String, Array, Object, RegExp, Number }; vm.createContext(c); vm.runInContext(src, c);
  ok('finish_reason: top-level', c.DPP_FINISH_REASON_331044({ finish_reason: 'rate_limit_reached', v: '' }) === 'rate_limit_reached');
  ok('finish_reason: nested in v / BATCH', c.DPP_FINISH_REASON_331044({ o: 'BATCH', v: [{ p: 'response/content', v: 'x' }, { v: { finish_reason: 'Rate_Limit_Reached' } }] }) === 'rate_limit_reached');
  ok('finish_reason: nested in error', c.DPP_FINISH_REASON_331044({ error: { finish_reason: 'generation_err' } }) === 'generation_err');
  ok('finish_reason: free text rejected', c.DPP_FINISH_REASON_331044({ finish_reason: 'rate limit reached; see https://x' }) === null && c.DPP_FINISH_REASON_331044({ finish_reason: 'a'.repeat(41) }) === null);
  ok('finish_reason: non-string / absent -> null', c.DPP_FINISH_REASON_331044({ finish_reason: 3 }) === null && c.DPP_FINISH_REASON_331044({ p: 'response/status', v: 'FINISHED' }) === null && c.DPP_FINISH_REASON_331044(null) === null);
  const frame = c.DPP_STREAM_DIAG_FRAME_33({ p: 'response/content', o: 'APPEND', v: 'TOP_SECRET_TEXT', finish_reason: 'rate_limit_reached' }, { type: 'message' });
  ok('diag frame carries finish and no content', frame.finish === 'rate_limit_reached' && !JSON.stringify(frame).includes('TOP_SECRET_TEXT'));
  ok('diag frame without finish_reason has no finish key', !('finish' in c.DPP_STREAM_DIAG_FRAME_33({ p: 'response/status', v: 'FINISHED' }, { type: 'message' })));
  ok('last finish reason from stream events', c.DPP_LAST_FINISH_REASON_331044([{ event: 'message' }, { event: 'message', finish: 'rate_limit_reached' }, { event: 'close' }]) === 'rate_limit_reached' && c.DPP_LAST_FINISH_REASON_331044([{ event: 'message' }]) === null && c.DPP_LAST_FINISH_REASON_331044(undefined) === null);
  ok('rate-limit stop message zh/en', /rate_limit_reached/.test(c.DPP_RATE_LIMIT_STOP_331044('zh-CN')) && /限流/.test(c.DPP_RATE_LIMIT_STOP_331044('zh-CN')) && /rate limited/.test(c.DPP_RATE_LIMIT_STOP_331044('en')));
  // HR(): the empty rate-limited attempt is not replayed
  const i0 = cjs.indexOf('async function HR('), i1 = cjs.indexOf('function UR(', i0);
  const hrCode = cjs.slice(i0, i1);
  async function runHr(sequence, withHelpers) {
    let calls = 0;
    const ctx = { console, AbortController, DOMException, setTimeout, clearTimeout, Promise, JSON, String, Object, Array, RegExp, Number, Math, Error,
      zR: 2, IR: () => ({ signal: new AbortController().signal, clear() {}, timedOut: () => false }), LR: async () => {},
      WL: async (e, handlers) => { const item = sequence[calls++]; if (item.throw) throw item.throw; if (item.text) handlers.onTextChunk(item.text, item.text); return { assistantText: item.text || '', responseMessageId: item.responseMessageId ?? null, requestMessageId: item.requestMessageId ?? null, finished: !!item.finished, dppStreamEvents: item.events ?? [] }; } };
    vm.createContext(ctx); vm.runInContext(hrCode + (withHelpers ? grabFn(cjs, 'DPP_LAST_FINISH_REASON_331044') + grabFn(cjs, 'DPP_RATE_LIMIT_STOP_331044') : ''), ctx);
    let out = null, err = null;
    try { out = await ctx.HR({}, { onTextChunk() {}, onReasoningChunk() {} }, new AbortController().signal); } catch (e) { err = e; }
    return { calls, out, err };
  }
  (async () => {
    let r = await runHr([{ finished: false, events: [{ event: 'message', finish: 'rate_limit_reached' }] }, { finished: true, responseMessageId: 9 }], true);
    ok('HR: empty rate-limited stream stops after ONE attempt with an explicit non-retryable error', r.calls === 1 && r.err && r.err.dppNoRetry337 === true && r.err.dppRateLimited331044 === true && /rate_limit_reached/.test(r.err.message), String(r.calls));
    r = await runHr([{ finished: false, events: [{ event: 'message', finish: 'rate_limit_reached' }], responseMessageId: 5 }], true);
    ok('HR: rate-limited stream with ids also stops explicitly', r.calls === 1 && r.err && r.err.dppRateLimited331044 === true);
    r = await runHr([{ finished: false, events: [{ event: 'message' }] }, { finished: true, responseMessageId: 9, events: [{ p: 'response/status', v: 'FINISHED' }] }], true);
    ok('HR: plain empty EOF is still replayed once (.33 behaviour unchanged)', r.calls === 2 && r.out && r.out.finished === true);
    r = await runHr([{ finished: false, text: 'partial', events: [{ event: 'message', finish: 'rate_limit_reached' }] }], true);
    ok('HR: partial text + rate limit is NOT thrown away (returned as incomplete)', r.calls === 1 && r.out && r.out.finished === false && !r.err);
    r = await runHr([{ finished: false, events: [{ event: 'message', finish: 'rate_limit_reached' }] }, { finished: true, responseMessageId: 9 }], false);
    ok('HR: without the helpers loaded (older suites slice HR alone) behaviour degrades to the .33 replay', r.calls === 2 && !r.err);
    // turn_diag whitelist + literals
    const fn = grabFn(cjs, 'DPP_RECORD_AGENT_TURN_DIAG_331021');
    const d = { rows: [], Date, JSON, Number, String, Array, Object, DPP_NUM_331018: v => (typeof v === 'number' && Number.isFinite(v) ? v : null), DPP_DIAG_BATCH_APPEND_331022: (k, lim, row) => { d.rows.push(row); return Promise.resolve(); }, DPP_AGENT_TURN_DIAG_KEY_331021: 'k' };
    vm.createContext(d); vm.runInContext(fn, d);
    d.DPP_RECORD_AGENT_TURN_DIAG_331021({ stage: 'provider_terminal', finishReason: 'rate_limit_reached' });
    d.DPP_RECORD_AGENT_TURN_DIAG_331021({ stage: 'provider_terminal' });
    d.DPP_RECORD_AGENT_TURN_DIAG_331021({ stage: 'provider_terminal', finishReason: { x: 1 } });
    ok('turn_diag whitelist persists finishReason (string only)', d.rows[0].finishReason === 'rate_limit_reached' && d.rows[1].finishReason === null && d.rows[2].finishReason === null);
    ok('provider_terminal payload carries finishReason', cjs.includes('finishReason:typeof DPP_LAST_FINISH_REASON_331044==`function`?DPP_LAST_FINISH_REASON_331044(ie?.streamEvents):null,'));
    ok('existing .25 generic limit untouched', cjs.includes('q(`generic_continuation_limit_331025`,!0)') && cjs.includes('DPP_GENERIC_NUDGE_MAX_331025=3'));
    ok('upload gate untouched', bg.includes('function DPP_REQUIRE_UPLOAD_CONTEXT_V7('));
    const mf = JSON.parse(fs.readFileSync(path.join(root, 'manifest.json'), 'utf8'));
    ok('manifest 1.14.0.49 / Fix 3.3.10.44', mf.version === '1.14.0.49' && mf.version_name === '1.14.0 ShunCode MCP Fix 3.3.10.44');
    console.log((fail ? 'FAIL ' : 'PASS ') + pass + '/' + (pass + fail));
    process.exit(fail ? 1 : 0);
  })();
}
