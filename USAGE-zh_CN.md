# DeepSeek++ ShunCode MCP Fix3 · 安装、配置与使用指南

适用版本：**1.14.0.39 / Fix 3.3.10.34**（2026-09-16）

---

## 1. 安装（Edge / Chrome 解压加载）

1. 下载 Release 里的 `DeepSeekPP-1.14.0-ShunCode-MCP-Fix3.3.10.34.zip`，解压到一个**固定目录**（例如 `D:\learn\DeepSeekPP-1.14.0-ShunCode-MCP-Fix3`）。以后升级直接覆盖这个目录即可。
2. 打开 `edge://extensions`（Chrome 为 `chrome://extensions`）→ 右上角开启 **开发人员模式** → **加载解压缩的扩展** → 选择上面的目录。
3. 若已装过旧版：在扩展卡片上点 **重新加载**，然后**关闭所有已打开的 DeepSeek 标签页**再新开（旧标签页里跑的还是旧脚本）。
4. 扩展卡片上应显示版本 `1.14.0.39`，名称带 `Fix 3.3.10.34`。

---

## 2. 首次配置（侧边栏 → MCP）

### 2.1 添加 ShunCode 服务器
- 侧边栏 → **MCP** → **添加服务器**
- 传输：`Streamable HTTP`
- URL：ShunCode 给你的 `https://…/mcp/<token>` 地址
- 保存后点 **测试连接**，状态应为 `ready`，工具数 15。

### 2.2 工具暴露模式（最重要的一项）
在服务器详情里有 **工具暴露** 下拉框，三个选项：

| 选项 | 含义 | 建议 |
|---|---|---|
| **直接：展示全部工具** | 15 个工具 Schema 全部进入 Prompt，模型可直接调用 | **推荐**。Prompt 约多 1 万字符，但最稳 |
| 自适应：展示相关工具 | 按你的提示词关键词挑 5 个直连，其余走 `mcp_discover` | `.33` 起已内置核心工具保底（`run_command / get_command_output / read_files` 必在），可用 |
| 按需：仅能力目录 | 全部工具走 discover → invoke 两步 | 不建议，步数翻倍 |

> 提示：当所有已启用服务器的工具总量很大（描述符总成本 > 28000 字符）时，扩展会自动把「直接」降级为「自适应」。只启用 ShunCode 一个服务器时不会触发。

### 2.3 置顶（Pin）
自适应模式下可在工具列表点 **置顶**，被置顶的工具在 5 个直连槽位里优先。建议置顶：`run_command`、`get_command_output`、`read_files`、`apply_patch`。（`.33` 起即使不置顶，这几个也有保底权重。）

### 2.4 执行权限
- 服务器详情 → **执行**：`启用`，模式 `auto`（自动执行，不逐条确认）。
- 允许列表保持 `all`。

### 2.5 其它建议
- 「Shell Local」「Multimodal Vision」两个内置服务器若用不到就保持禁用，减少 Prompt 体积。
- 设置 → 提示词：`systemPromptEnabled` 保持开启（Agent 规则靠它注入）。

---

## 3. 使用方法

### 3.1 正常发起任务
在 DeepSeek 网页直接描述任务即可，例如：
> 在 D:\proj 里找出所有引用 read_image 的文件并列出行号，然后把结果写到 docs/notes.md。

扩展会把 ShunCode 工具注入请求；模型输出工具调用后，扩展自动执行并把结果回填，形成多步 Agent 循环。页面下方会出现 **状态条**（`运行中 · 第 N 步 · 自动续跑额度剩余 M 轮`）。

### 3.2 Agent 运行期间**不要发新消息**（最常见的误操作）
- 状态条显示「运行中 / 正在执行工具 / 本轮完成，正在决定下一步」时，**等待**。每一步（模型思考 + 工具执行）通常 10–20 秒。
- 如果此时发送任何消息（包括「继续」「怎么样了」），扩展会**放弃当前任务**去处理新消息。`.34` 起页面会明确提示：「已被你的新消息中断……等状态栏显示结束后再发“继续”可恢复」。
- 真的想停：点状态条上的 **停止**。

### 3.3 任务中断后如何恢复
- 状态条变为「运行已停止 · 任务未完成」或「运行已暂停」后，发送 **`继续`**（或 `继续直到任务完成`）。
- 扩展会接上上次的检查点（已执行的工具结果不重跑）。
- 「继续直到任务完成」会把自动续跑额度提高到 128 轮，适合长任务。

### 3.4 如何判断它是否真的在干活
- 状态条里的工具计数在涨；
- 展开消息里的工具块能看到 `run_command` 的命令与输出；
- 模型正文里若出现类似 `<run_command>` 的原始标签，说明该工具未注入（`.33` 已修，若再见到请反馈）。

### 3.5 常见现象与处理

| 现象 | 原因 | 处理 |
|---|---|---|
| 「已被你的新消息中断」 | 运行中发了消息 | 等结束再发「继续」 |
| 「MCP 暂时断连 / mcp_network_error」 | ngrok 隧道瞬断 | 直接发「继续」，`.31` 起会自动重发最多 3 次 |
| 「服务器暂不可用」 | DeepSeek 后端 `generation_err` | 扩展自动做 compact → passthrough 重试；反复出现就新开会话 |
| 页面崩溃 / 白屏 | 长会话 DOM 压力 | 硬刷新即可（`.30` 起有 DOM 保险丝）；建议长任务分会话 |
| 命令明明成功却显示「Command failed」 | `.33` 及以前对输出正文误判 | `.34` 已修 |

---

## 4. 出问题时怎么取证
1. 不要关标签页，不要清缓存。
2. 复制 `…\Edge\User Data\Default\Local Extension Settings\<扩展ID>` 整个目录到别处。
3. 关键键：`dpp_inline_agent_traces`（每次 Agent 循环的步骤）、`dpp_agent_turn_diag_331021`（每轮决策）、`dpp_request_preflight_diag_331018`（你的每次发送）、`dpp_web_response_diag_331015`（注入了哪些工具、HTTP 状态）。
4. 把目录交给维护者，或对 Agent 说「查看新日志」。

---

## 5. 升级与回退
- 升级：解压新 ZIP 覆盖同一目录 → `edge://extensions` 重新加载 → 关旧标签页。
- 回退：用上一个 Release 的 ZIP 覆盖即可；数据（服务器配置、记忆、历史）存在浏览器里，不受影响。
- 每个版本都有 `tools/apply-fix3310NN.py` 补丁器和 `fix3310NN-*-selftest.js` 自测；`node fix3310NN-*-selftest.js` 应全绿。
